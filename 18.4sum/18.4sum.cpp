                int hi=n-1;
                int lo =j+1;
                int k=nums[j];
                int s=nums[i];
                if(j>i+1 && nums[j]==nums[j-1]) continue;
            for(int j=i+1; j<n-2;j++){
            if(i>0 && nums[i]==nums[i-1]) continue;
        for(int i=0;i<n-3;i++){
        int n=nums.size();
        sort(nums.begin(),nums.end());
         vector<vector<int>>ans;
                while(lo<hi){
                    vector<int>v;
                    if(s+k+nums[lo]+nums[hi]==target){

