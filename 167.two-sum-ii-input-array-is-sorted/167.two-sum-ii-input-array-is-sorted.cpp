                sumOf[0]=lo+1;
                sumOf[1]=hi+1;
                break;

            }
            else if(sum<target){
                lo++;
            }
            else {
            int sum = numbers[lo]+numbers[hi];
            if(sum==target){
        int hi =n-1;
        while(lo<hi){
    vector<int> twoSum(vector<int>& numbers, int target) {
        vector<int>sumOf(2);
        int n = numbers.size();
        int lo=0;
public:
class Solution {

