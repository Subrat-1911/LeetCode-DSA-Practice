            }
                }
                    mindx=i;
            if(min==0) break;
            else nums[mindx]=-nums[mindx];
            k--;
                    min=nums[i];
            for(int i=0;i<n;i++){
                if(nums[i]<min){
        while(k>0){
        
        int n=nums.size();
    int largestSumAfterKNegations(vector<int>& nums, int k) {
public:
            int min=INT_MAX;
            int mindx=-1;

