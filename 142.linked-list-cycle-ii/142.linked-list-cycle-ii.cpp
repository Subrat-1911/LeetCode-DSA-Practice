        }
        if(flag == false) return NULL;
    

            }
                break;
            if(slow == fast){
                flag = true;
            slow = slow->next;
            fast = fast->next->next;
        bool flag = false; // no cicle
        while(fast != NULL && fast->next != NULL){
        ListNode* slow = head;
        ListNode* fast = head;
    ListNode *detectCycle(ListNode *head) {
class Solution {
public:
 */
 * };

