                //right side
                lo=mid+1;
            }
            else {
            if(nums[mid] > nums[hi]){

            int mid = lo+(hi-lo)/2;
            // if(nums[mid]<min){
            //     min=nums[mid];
            // }
        //int min=INT_MAX;
        while(lo<hi){
        int lo =0;
        int hi = n-1;
        int n = nums.size();

    int findMin(vector<int>& nums) {
public:
class Solution {

