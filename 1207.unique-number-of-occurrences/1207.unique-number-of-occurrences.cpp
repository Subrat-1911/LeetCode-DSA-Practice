        }
        for(auto freq:mp){
            int f=freq.second;
        }
            if(s.find(f)!=s.end()){ // means freq found
        unordered_set<int>s;
                return false;
            }
            else{
                s.insert(f);
            }
        return true;
    }
};
            mp[arr[i]]++;
        for(int i=0;i<n;i++){
        unordered_map<int,int>mp;

