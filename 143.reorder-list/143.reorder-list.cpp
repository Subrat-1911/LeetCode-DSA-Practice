        while(a!=NULL && b!=NULL){
            c->next=a;
        ListNode* a=head;
            c=c->next;
            a=a->next;
            c->next=b;
            c=c->next;
            b=b->next;

        ListNode* b=newHead;
        }
        c->next=a;
        head=dummy->next;
    }
};
        ListNode* c=dummy;
        ListNode* dummy=new ListNode(100);
        newHead=Reverse(newHead);

