        }
            nums.push_back(rev);
            int rev = revers(nums[i]);
        for(int i=0;i<n;i++){
        unordered_set<int> s;
        int n = nums.size();
    int countDistinctIntegers(vector<int>& nums) {
    }
            
        return rev;
        for(int i=0;i<nums.size();i++){
            s.insert(nums[i]);
        }
        
        return s.size();
    }
};

