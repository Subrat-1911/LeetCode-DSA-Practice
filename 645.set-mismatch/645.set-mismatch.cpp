            if(nums[i]==nums[i+1]){
        }
                dup=nums[i];
            }
                break;
        for(int i=0;i<n;i++){
            sum+=nums[i];
        int sum=0;
        ans.push_back(dup);
        }
        int orgSum=n*(n+1)/2;
        int mis=(orgSum-(sum-dup));
        ans.push_back(mis);
        return ans;

    }
};

