            if(nums[i]==nums[i+1]){
        }
                dup=nums[i];
            }
                break;
        for(int i=0;i<n;i++){
            sum+=nums[i];
        int sum=0;
        ans.push_back(dup);
        for(int i=0;i<n-1;i++){
        int n= nums.size();
        int dup=-1;
        //sort(nums.begin(),nums.end());
public:
    vector<int> findErrorNums(vector<int>& nums) {
        vector<int> ans;
class Solution {

