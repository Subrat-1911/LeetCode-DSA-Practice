            int sum=Divisor(nums,mid);
            if(sum>threshold){
                lo=mid+1;
            }
            else{
                hi=mid;
            }
        }
        return lo;

        
    }
            int mid=lo+(hi-lo)/2;
        while(lo<hi){
        int lo=1;
        }
            }

