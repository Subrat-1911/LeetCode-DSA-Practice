
        }
                hi = mid;
            else
                lo = mid + 1;
            if (nums[mid] <= 0)
            int mid = lo + (hi - lo) / 2;


        while (lo < hi) {
        // return max(neg,pos);
         // First positive index
        int lo = 0, hi = n - 1;
        //     else if(nums[i]>0) pos++;
        // }
        //     if(nums[i]<0) neg++;
        // for(int i=0;i<n;i++){

