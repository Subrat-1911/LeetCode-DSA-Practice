            }
                if(abs(sum-target)<abs(closet-target)){
                    closet=sum;
                }
                if(sum==target) return target;
            
        }
                int sum=s+nums[lo]+nums[hi];
            while(lo<hi){
            closet=s+nums[lo]+nums[hi];
            int hi=n-1;
            int lo =i+1;
            int s=nums[i];
        for(int i=0;i<n-2;i++){
        int closet=0;
        int n=nums.size();
        sort(nums.begin(),nums.end());
        return closet;
         
        
                lo++;
                hi--;

